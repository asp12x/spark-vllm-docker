# Recipe: Qwen3.6-35B-A3B-NVFP4-Solo
# Single DGX Spark (GB10) serving of nvidia/Qwen3.6-35B-A3B-NVFP4.
#
# Validated 2026-07-14 on Spark (OTA2607 July, driver 580.159.03) with the
# vllm/vllm-openai:nightly image built 2026-07-14:
#   * 106 tok/s single-stream, 264 tok/s @ N=8 concurrency
#   * 78/121 GB used (42 GB free), 3.49M-token KV pool, 13.3x concurrency @ 262K ctx
#   * character-perfect OCR on the vision path (multimodal: text + image + video in)
# Flags follow the HuggingFace model-card DGX Spark recipe VERBATIM
# (TP=1, fp8 KV, flashinfer attention, marlin MoE, MTP-3 speculative decoding).
#
# NOTE: the sibling recipe qwen3.6-35b-a3b-nvfp4.yaml is a 2-GPU / cluster config
# (tensor_parallel: 2, ray executor) and will NOT run on a single GB10 -- this
# "-solo" variant is the verified single-Spark choice.

recipe_version: "1"
name: Qwen3.6-35B-A3B-NVFP4-Solo
description: Single-Spark vLLM serving of nvidia/Qwen3.6-35B-A3B-NVFP4 (marlin MoE, fp8 KV, MTP-3); multimodal text+image+video, 262K context

# HuggingFace model to download (ungated -- no token required)
model: nvidia/Qwen3.6-35B-A3B-NVFP4

# Container image this config was validated against
container: vllm/vllm-openai:nightly
cluster_only: false
solo_only: true

# Default settings (overridable via CLI)
defaults:
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 1
  gpu_memory_utilization: 0.4
  max_model_len: 262144
  max_num_seqs: 4
  max_num_batched_tokens: 8192

# The vLLM serve command template
command: |
  vllm serve nvidia/Qwen3.6-35B-A3B-NVFP4 \
    --served-model-name qwen3.6-35b-a3b:nvfp4 \
    --host {host} \
    --port {port} \
    --tensor-parallel-size {tensor_parallel} \
    --trust-remote-code \
    --kv-cache-dtype fp8 \
    --attention-backend flashinfer \
    --moe-backend marlin \
    --gpu-memory-utilization {gpu_memory_utilization} \
    --max-model-len {max_model_len} \
    --max-num-seqs {max_num_seqs} \
    --max-num-batched-tokens {max_num_batched_tokens} \
    --enable-chunked-prefill \
    --async-scheduling \
    --enable-prefix-caching \
    --speculative-config '{{"method":"mtp","num_speculative_tokens":3,"moe_backend":"triton"}}' \
    --load-format fastsafetensors \
    --reasoning-parser qwen3 \
    --tool-call-parser qwen3_xml \
    --enable-auto-tool-choice
